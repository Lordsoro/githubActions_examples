name: 'Modify Readme Action'
description: 'Action to modify the readme.md file with a random meme text'
inputs:
  positive_phrase:
    description: 'A positive phrase'
    required: true
  negative_phrase:
    description: 'A negative phrase'
    required: true
  result_tests:
    description: 'Result of the test execution'
    required: true
outputs:
  message:
    description: 'Response message'
